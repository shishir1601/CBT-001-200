   F   &DThis dataset contains sample ISPF dialogs and EDIT macros as follows: |
GA dialog which can be used to keep track of updates to the TSO broadcast gdatas
et. | A dialog which can be used to edit an ISPF command table, including ISRCMD
S and gISPCMDS. | A dialog/panel which facilitates the use of the PC3270 file tr
ansfer program  while in ISPF. |{A program to display the logo of the submitter
via GDDM and ISPF. |DA dialog which will end the ISPF session (if not in split s
creen) and  (optionally) log the user off TSO and / or log the same or another u
ser on TSO. |EA son-of-spy MVS console dialog which will display the contents of
 any GMCS console via an ISPF table, and facilitate the entry of MVS commands. |
qA GRS/ENQ display dialog. | An ISPF edit macro which facilitates the entry of d
ata into ISPF EDIT via the  Work Station Control Copy function of PC 3270s. |IAn
 edit macro which will list (on a system printer) the data being edited,  includ
ing changes, without saving the data. |FAn edit macro which displays (via NOTE a
nd MSG lines) information about tthe dataset and member being edited. | An edit
macro which uses DCF (SCRIPT) to spell check the data being edited. | Mods to th
e QUEUE command to add ISPF support | A utility to copy the initial TSO command
from UADS to RACF. | An ISPF dialog to facilitate update of TSO logon informatio
n.
